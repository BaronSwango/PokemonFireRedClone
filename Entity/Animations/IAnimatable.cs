namespace PokemonFireRedClone {
    public interface IAnimatable
    {
        bool IsMovementRestricted { get; }
        // void OnAnimationStart(IEntityAnimation animation);
        // void OnAnimationComplete(IEntityAnimation animation);
    }
}